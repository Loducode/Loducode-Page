---
interface Props {
  class?: string;
  delay?: string;
}

const { class: className = '', delay = '0ms' } = Astro.props;
---
<div class={`scroll-reveal ${className}`} style={`transition-delay: ${delay}`}>
  <slot />
</div>
